# Expo SDK 54 ships React 19.1.0; Clerk Expo's peer accepts ~19.1.4+. The diff
# is a stale peer constraint, not a real incompatibility, so allow legacy
# peer-dep resolution project-wide.
legacy-peer-deps=true
